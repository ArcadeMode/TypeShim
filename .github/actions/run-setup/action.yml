name: Setup 
description: Sets up the environment

runs:
  using: composite
  steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        global-json-file: src/global.json
    
    - name: Setup Node
      uses: actions/setup-node@v7
      with:
        node-version: '24'
        cache: npm
        cache-dependency-path: src/TypeShim.E2E/vitest/package-lock.json